version: '3.8'
services:
  simple-producer:
    build:
      context: ./dockerfile_folder
      dockerfile: Dockerfile.producer 
    container_name: kafka-producer
    volumes:
      - ./kafka_producers/0519_kafka_producer_latest.py/:/app/0519_kafka_producer_latest.py

    networks:
      - my-simple-network

networks:
  my-simple-network:
    external: true

